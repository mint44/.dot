local colors = require("colors")

local separator = sbar.add("item", {
  position = "left",
  icon = {
    string = "│",
  },
  background = {
    drawing = false,
  },
  label = {
    drawing = false,
  },
})
